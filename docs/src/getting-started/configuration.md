# Configuration

<!-- OUTLINE — replace with prose.
Source: README-old.md "Configuration" (L67–111).

- The one required file: config.toml listing your projects.
- Location table (Linux / macOS / Windows); tip: run any command with no config
  and the error names the exact path.
- Minimal example with two projects.
- The `#:schema` line + tombi recommendation.
- Mention (one line each, link to reference): per-project `devcontainer`
  overrides, `worktreeFolder`.
- Pointer to the full config.toml reference chapter.
-->
