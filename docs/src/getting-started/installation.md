# Installation

<!-- OUTLINE — replace with prose.
Source: README-old.md "Prerequisites" (L26–32), "Installation" (L36–43).

- Prerequisites:
  - Required: a git repository.
  - For devcontainer features: modern docker (+ compose).
  - For DNS/proxy: one-time system setup, covered in the DNS chapter.
- Install methods:
  - GitHub releases link.
  - mise: `mise install github:paholg/devconcurrent`.
  - nix flake.
- GAP (new content): minimum docker/compose versions, if known.
- GAP (new content): how to upgrade, and where to watch for breaking changes
  (release notes).
-->
