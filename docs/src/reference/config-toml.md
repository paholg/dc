# Configuration Reference

Devconcurrent is configured by a simple toml file.

<!-- TODO: We should have a CLI command to show the config file path -->

You can override the default path by setting the `DEVCONCURRENT_CONFIG` environment
variable; this should be the path to the directory containing `config.toml`.

{{#include generated/config-toml.md}}
