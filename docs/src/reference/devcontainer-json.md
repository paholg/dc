# Devcontainer Configuration Reference

This is a reproduction of the full
[`devcontainer.json`](https://containers.dev/implementors/json_reference/)
specification, as supported by devconcurrent, with our customizations included.
It is formatted as TOML, but the same options can be set as JSON in
`devcontainer.json`.

See the reference spec for more details.

{{#include generated/devcontainer-json.md}}
