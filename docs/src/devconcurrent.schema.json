{
  "$id": "https://devconcurrent.paholg.com/devconcurrent.schema.json",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Config",
  "type": "object",
  "properties": {
    "projects": {
      "description": "Configured projects by name.",
      "type": "object",
      "propertyNames": {
        "type": "string",
        "pattern": "^[A-Za-z0-9_-]+$"
      },
      "additionalProperties": {
        "$ref": "#/definitions/Project"
      }
    },
    "proxy": {
      "description": "Global proxy settings.",
      "default": {
        "port": 43770,
        "caRoot": null
      },
      "allOf": [
        {
          "$ref": "#/definitions/ProxyGlobal"
        }
      ]
    },
    "shell": {
      "description": "Shell-integration settings.",
      "default": {
        "exportEnv": false
      },
      "allOf": [
        {
          "$ref": "#/definitions/ShellGlobal"
        }
      ]
    }
  },
  "x-tombi-toml-version": "v1.1.0",
  "definitions": {
    "Project": {
      "description": "A devconcurrent-enabled project.",
      "type": "object",
      "properties": {
        "path": {
          "description": "The project location on your host.",
          "type": "string"
        },
        "worktreeFolder": {
          "description": "The directory where devconcurrent will place worktrees. Defaults to the platform data\ndirectory. This is also settable in the devcontainer, but it's available here for projects\nthat don't use devcontainers.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "devcontainer": {
          "description": "Any of the options from `devcontainer.json` (<https://containers.dev/implementors/json_reference/>),\nas per-user overrides. These are merged with the project's `devcontainer.json`, with arrays\nconcatenated and this file winning conflicts.",
          "anyOf": [
            {
              "$ref": "#/definitions/DevcontainerConfig"
            },
            {
              "type": "null"
            }
          ]
        }
      },
      "required": [
        "path"
      ]
    },
    "DevcontainerConfig": {
      "description": "Devcontainer config from devcontainer.json.",
      "type": "object",
      "properties": {
        "dockerComposeFile": {
          "description": "The name of the docker-compose file(s) used to start the services.",
          "default": [],
          "allOf": [
            {
              "$ref": "#/definitions/OneOrMany(Template,PreferOne)"
            }
          ]
        },
        "service": {
          "description": "The service you want to work on. This is considered the primary container for your dev\nenvironment which your editor will connect to.",
          "type": "string",
          "default": ""
        },
        "runServices": {
          "description": "An array of services that should be started and stopped.",
          "type": [
            "array",
            "null"
          ],
          "items": {
            "type": "string"
          },
          "default": null
        },
        "workspaceFolder": {
          "description": "The path of the workspace folder inside the container. This is typically the target path of\na volume mount in the docker-compose.yml.",
          "default": "",
          "allOf": [
            {
              "$ref": "#/definitions/Template"
            }
          ]
        },
        "shutdownAction": {
          "description": "Action to take when the user disconnects from the primary container in their editor. The\ndefault is to stop all of the compose containers.",
          "default": "stopCompose",
          "allOf": [
            {
              "$ref": "#/definitions/ComposeShutdownAction"
            }
          ]
        },
        "overrideCommand": {
          "description": "Whether to overwrite the command specified in the image. The default is false.",
          "type": "boolean",
          "default": false
        },
        "$schema": {
          "description": "The JSON schema of the devcontainer.json file.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "name": {
          "description": "A name for the dev container which can be displayed to the user.",
          "anyOf": [
            {
              "$ref": "#/definitions/Template"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "features": {
          "description": "Features to add to the dev container.",
          "default": null
        },
        "overrideFeatureInstallOrder": {
          "description": "Array consisting of the Feature id (without the semantic version) of Features in the order\nthe user wants them to be installed.",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "secrets": {
          "default": null
        },
        "forwardPorts": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/ForwardPort"
          },
          "default": []
        },
        "portsAttributes": {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/PortAttributes"
          },
          "default": {}
        },
        "otherPortsAttributes": {
          "description": "Set default properties that are applied to all ports that don't get properties from the\nsetting `remote.portsAttributes`",
          "anyOf": [
            {
              "$ref": "#/definitions/PortAttributes"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "updateRemoteUserUid": {
          "description": "Controls whether on Linux the container's user should be updated with the local user's UID\nand GID. On by default when opening from a local folder.",
          "type": [
            "boolean",
            "null"
          ],
          "default": null
        },
        "containerEnv": {
          "description": "Container environment variables.",
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/Template"
          },
          "default": {}
        },
        "containerUser": {
          "description": "The user the container will be started with. The default is the user on the Docker image.",
          "anyOf": [
            {
              "$ref": "#/definitions/Template"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "mounts": {
          "type": "array",
          "items": {
            "$ref": "#/definitions/MountEntry"
          },
          "default": []
        },
        "init": {
          "description": "Passes the --init flag when creating the dev container.",
          "type": [
            "boolean",
            "null"
          ],
          "default": null
        },
        "privileged": {
          "description": "Passes the --privileged flag when creating the dev container.",
          "type": [
            "boolean",
            "null"
          ],
          "default": null
        },
        "capAdd": {
          "description": "Passes docker capabilities to include when creating the dev container.",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "securityOpt": {
          "description": "Passes docker security options to include when creating the dev container.",
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": []
        },
        "remoteEnv": {
          "description": "Remote environment variables to set for processes spawned in the\ncontainer including lifecycle scripts and any remote editor/IDE server\nprocess.",
          "type": "object",
          "additionalProperties": {
            "anyOf": [
              {
                "$ref": "#/definitions/Template"
              },
              {
                "type": "null"
              }
            ]
          },
          "default": {}
        },
        "remoteUser": {
          "description": "The username to use for spawning processes in the container including\nlifecycle scripts and any remote editor/IDE server process. The default\nis the same user as the container.",
          "anyOf": [
            {
              "$ref": "#/definitions/Template"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "initializeCommand": {
          "description": "A command to run locally (i.e Your host machine, cloud VM) before anything else. This\ncommand is run before \"onCreateCommand\".",
          "anyOf": [
            {
              "$ref": "#/definitions/LifecycleCommandTemplate"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "onCreateCommand": {
          "description": "A command to run when creating the container. This command is run after \"initializeCommand\"\nand before \"updateContentCommand\".",
          "anyOf": [
            {
              "$ref": "#/definitions/LifecycleCommandTemplate"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "updateContentCommand": {
          "description": "A command to run when creating the container and rerun when the workspace content was\nupdated while creating the container. This command is run after \"onCreateCommand\" and before\n\"postCreateCommand\".",
          "anyOf": [
            {
              "$ref": "#/definitions/LifecycleCommandTemplate"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "postCreateCommand": {
          "description": "A command to run after creating the container. This command is run after\n\"updateContentCommand\" and before \"postStartCommand\".",
          "anyOf": [
            {
              "$ref": "#/definitions/LifecycleCommandTemplate"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "postStartCommand": {
          "description": "A command to run after starting the container. This command is run after \"postCreateCommand\"\nand before \"postAttachCommand\".",
          "anyOf": [
            {
              "$ref": "#/definitions/LifecycleCommandTemplate"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "postAttachCommand": {
          "description": "A command to run when attaching to the container. This command is run after\n\"postStartCommand\".",
          "anyOf": [
            {
              "$ref": "#/definitions/LifecycleCommandTemplate"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "waitFor": {
          "description": "The user command to wait for before continuing execution in the background while the UI is\nstarting up.",
          "default": "updateContentCommand",
          "allOf": [
            {
              "$ref": "#/definitions/WaitFor"
            }
          ]
        },
        "userEnvProbe": {
          "description": "User environment probe to run.",
          "default": "loginInteractiveShell",
          "allOf": [
            {
              "$ref": "#/definitions/UserEnvProbe"
            }
          ]
        },
        "hostRequirements": {
          "description": "Host hardware requirements.",
          "anyOf": [
            {
              "$ref": "#/definitions/HostRequirements"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "customizations": {
          "description": "Tool-specific configuration. Each tool should use a JSON object subproperty with a unique\nname to group its customizations.",
          "default": {
            "devconcurrent": {
              "worktreeFolder": null,
              "mountGit": null,
              "proxy": {
                "enable": false,
                "hostname": null,
                "services": {}
              },
              "env": {}
            }
          },
          "allOf": [
            {
              "$ref": "#/definitions/Customizations"
            }
          ]
        }
      }
    },
    "OneOrMany(Template,PreferOne)": {
      "anyOf": [
        {
          "$ref": "#/definitions/Template"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/definitions/Template"
          }
        }
      ]
    },
    "Template": {
      "description": "A string that may contain `${...}` variable substitutions. Supported variables: `${localEnv:VAR[:default]}`, `${containerEnv:VAR[:default]}`, `${localWorkspaceFolder}`, `${containerWorkspaceFolder}`, `${localWorkspaceFolderBasename}`, `${containerWorkspaceFolderBasename}`, `${devcontainerId}`. See https://containers.dev/implementors/json_reference/#variables-in-devcontainerjson.",
      "type": "string"
    },
    "ComposeShutdownAction": {
      "type": "string",
      "enum": [
        "none",
        "stopCompose"
      ]
    },
    "ForwardPort": {
      "oneOf": [
        {
          "description": "A port to forward to the primary container.",
          "type": "integer",
          "minimum": 0,
          "maximum": 65535
        },
        {
          "description": "A `service:port` mapping selecting a compose service.",
          "type": "string",
          "pattern": "^[^:]+:\\d+$"
        }
      ]
    },
    "PortAttributes": {
      "type": "object",
      "properties": {
        "onAutoForward": {
          "default": "notify",
          "allOf": [
            {
              "$ref": "#/definitions/OnAutoForward"
            }
          ]
        },
        "elevateIfNeeded": {
          "type": "boolean",
          "default": false
        },
        "label": {
          "type": "string",
          "default": "Application"
        },
        "protocol": {
          "default": "http",
          "allOf": [
            {
              "$ref": "#/definitions/Protocol"
            }
          ]
        },
        "requireLocalPort": {
          "type": "boolean",
          "default": false
        }
      }
    },
    "OnAutoForward": {
      "type": "string",
      "enum": [
        "notify",
        "openBrowser",
        "openBrowserOnce",
        "openPreview",
        "silent",
        "ignore"
      ]
    },
    "Protocol": {
      "type": "string",
      "enum": [
        "http",
        "https"
      ]
    },
    "MountEntry": {
      "anyOf": [
        {
          "description": "Docker `--mount` short form: `type=bind,source=...,target=...`.",
          "allOf": [
            {
              "$ref": "#/definitions/Template"
            }
          ]
        },
        {
          "$ref": "#/definitions/Mount"
        }
      ]
    },
    "Mount": {
      "type": "object",
      "properties": {
        "type": {
          "$ref": "#/definitions/MountType"
        },
        "source": {
          "anyOf": [
            {
              "$ref": "#/definitions/Template"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "target": {
          "$ref": "#/definitions/Template"
        }
      },
      "required": [
        "type",
        "target"
      ]
    },
    "MountType": {
      "type": "string",
      "enum": [
        "bind",
        "volume"
      ]
    },
    "LifecycleCommandTemplate": {
      "description": "A lifecycle command as written in config, before `${...}` substitution.",
      "anyOf": [
        {
          "$ref": "#/definitions/CmdTemplate"
        },
        {
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/CmdTemplate"
          }
        }
      ]
    },
    "CmdTemplate": {
      "description": "A command as written in config, before `${...}` substitution.",
      "anyOf": [
        {
          "$ref": "#/definitions/Template"
        },
        {
          "type": "array",
          "items": {
            "$ref": "#/definitions/Template"
          }
        }
      ]
    },
    "WaitFor": {
      "type": "string",
      "enum": [
        "initializeCommand",
        "onCreateCommand",
        "updateContentCommand",
        "postCreateCommand",
        "postStartCommand"
      ]
    },
    "UserEnvProbe": {
      "type": "string",
      "enum": [
        "none",
        "loginShell",
        "loginInteractiveShell",
        "interactiveShell"
      ]
    },
    "HostRequirements": {
      "type": "object",
      "properties": {
        "cpus": {
          "description": "Number of required CPUs. Minimum 1.",
          "type": "integer",
          "format": "uint64",
          "minimum": 0,
          "default": 1
        },
        "memory": {
          "description": "Amount of required RAM in bytes. Supports units tb, gb, mb and kb.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "storage": {
          "description": "Amount of required RAM in bytes. Supports units tb, gb, mb and kb.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "gpu": {
          "default": false,
          "allOf": [
            {
              "$ref": "#/definitions/GpuRequirement"
            }
          ]
        }
      }
    },
    "GpuRequirement": {
      "anyOf": [
        {
          "type": "boolean"
        },
        {
          "$ref": "#/definitions/GpuOptional"
        },
        {
          "type": "object",
          "properties": {
            "cores": {
              "description": "Number of cores. Minimum 1.",
              "type": [
                "integer",
                "null"
              ],
              "format": "uint64",
              "minimum": 0
            },
            "memory": {
              "description": "Amount of required RAM in bytes. Supports units tb, gb, mb and kb.",
              "type": [
                "string",
                "null"
              ]
            }
          }
        }
      ]
    },
    "GpuOptional": {
      "type": "string",
      "enum": [
        "optional"
      ]
    },
    "Customizations": {
      "type": "object",
      "properties": {
        "devconcurrent": {
          "default": {
            "worktreeFolder": null,
            "mountGit": null,
            "proxy": {
              "enable": false,
              "hostname": null,
              "services": {}
            },
            "env": {}
          },
          "allOf": [
            {
              "$ref": "#/definitions/DcOptions"
            }
          ]
        }
      }
    },
    "DcOptions": {
      "type": "object",
      "properties": {
        "worktreeFolder": {
          "description": "The directory where devconcurrent will place worktrees.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        },
        "mountGit": {
          "description": "Whether to mount the project's git directory into each workspace's devcontainer.\n\nGit worktrees have a simple `.git` file that points to the actual `.git` directory. If that\ndirectory isn't available, then no git commands will work. By mounting it at its original\npath in the devcontainer, `git` should just work, both inside and out of the container.",
          "type": [
            "boolean",
            "null"
          ],
          "default": true
        },
        "proxy": {
          "description": "Configure DNS hostnames and HTTP proxy.",
          "default": {
            "enable": false,
            "hostname": null,
            "services": {}
          },
          "allOf": [
            {
              "$ref": "#/definitions/ProxyOptions"
            }
          ]
        },
        "env": {
          "description": "Define shell variables\n\nThese are rendered by `dc show env` or automatically set if `shell.exportEnv` is true.\n\nThe values are given by handlebars templates with the following:\n  * The `hostname` helper gives the hostname for a service.\n  * The following variables are populated: `project`, `workspace`, and `root`.",
          "type": "object",
          "propertyNames": {
            "description": "A shell variable name.",
            "type": "string",
            "pattern": "^[A-Za-z_][A-Za-z0-9_]*$"
          },
          "additionalProperties": {
            "$ref": "#/definitions/HandlebarsTemplate"
          },
          "examples": [
            {
              "BASE_URL": "{{ hostname 'app' }}",
              "DATABASE_URL": "postgres://postgres:postgres@{{hostname 'postgres'}}:5432/db"
            }
          ],
          "default": {}
        }
      }
    },
    "ProxyOptions": {
      "description": "Per-project proxy configuration.",
      "type": "object",
      "properties": {
        "enable": {
          "description": "Enable the devconcurrent DNS and HTTP proxy for this project.",
          "type": "boolean",
          "default": false
        },
        "hostname": {
          "description": "Handlebars template for the proxied hostname, used by every service\nthat does not set its own.\n\nAvailable variables:\n- `root` (bool) — whether this is the root workspace\n- `project` — project name\n- `workspace` — workspace name\n- `service` — name of the service from compose",
          "anyOf": [
            {
              "$ref": "#/definitions/HandlebarsTemplate"
            },
            {
              "type": "null"
            }
          ],
          "default": "{{workspace}}.{{service}}.test"
        },
        "services": {
          "description": "Configure proxy settings for each docker compose service.",
          "type": "object",
          "additionalProperties": {
            "$ref": "#/definitions/ProxyService"
          },
          "default": {}
        }
      }
    },
    "HandlebarsTemplate": {
      "description": "A Handlebars template. Hostname templates get `root` (bool), `project`, `workspace` and `service`; `env` templates get `root`, `project`, `workspace` and the `hostname` helper.",
      "type": "string"
    },
    "ProxyService": {
      "type": "object",
      "properties": {
        "hostname": {
          "description": "Handlebars template for this service's hostname. Overrides the\nproject-level `hostname`.",
          "anyOf": [
            {
              "$ref": "#/definitions/HandlebarsTemplate"
            },
            {
              "type": "null"
            }
          ],
          "default": null
        },
        "containerPort": {
          "description": "If set, devconcurrent will run an HTTP proxy on ports 80 and 443 to this port in your\ncontainer, performing TLS termination on 443.\n\nIf this service runs a web service, put its port here.\n\nAll ports other than 80 and 443 are forwarded raw to the service, whether\nthis is set or not.",
          "type": [
            "integer",
            "null"
          ],
          "format": "uint16",
          "minimum": 0,
          "maximum": 65535,
          "default": null
        }
      }
    },
    "ProxyGlobal": {
      "description": "Global user proxy settings.",
      "type": "object",
      "properties": {
        "port": {
          "description": "The DNS port the proxy listens on.",
          "type": "integer",
          "format": "uint16",
          "minimum": 0,
          "maximum": 65535,
          "default": 43770
        },
        "caRoot": {
          "description": "Path to your CA root directory on the host. Find it with `mkcert -CAROOT`.",
          "type": [
            "string",
            "null"
          ],
          "default": null
        }
      }
    },
    "ShellGlobal": {
      "description": "Global shell-integration settings, applied when you source\n`COMPLETE=<shell> devconcurrent`.",
      "type": "object",
      "properties": {
        "exportEnv": {
          "description": "Register a prompt hook to auto-set the variables from `customizations.devconcurrent.env`\nbased on your current working directory.",
          "type": "boolean",
          "default": false
        }
      }
    }
  }
}
