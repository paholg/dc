# Introduction

`devconcurrent` manages git worktrees plus devcontainers, with an optional DNS
server and TLS-terminating proxy.

This book is not written yet. For now, see the
[README](https://github.com/paholg/devconcurrent#readme).
