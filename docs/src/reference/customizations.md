# Devcontainer Customization Reference

The devcontainer spec supports customizations; we put them in
`customizations.devconcurrent`. Note that we also support overrides in
`config.toml`, so you could configure devconcurrent, or even a full
`devcontainer.json` in your personal config file for a repository that does not
have it set up.

{{#include generated/customizations.md}}
