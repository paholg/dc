[package]
name = "shared"
version = "0.0.0"
edition = "2024"
description = "Wire format types shared between the devconcurrent CLI and the devconcurrent-proxy service"
publish = false

[dependencies]
handlebars = "6.4.4"
http = "1.5.0"
indexmap = { workspace = true }
schemars = { version = "1.2.2", features = ["indexmap2", "preserve_order"] }
serde = { workspace = true }
serde_json = { workspace = true }
sha2 = "0.11.0"

[lints]
workspace = true
