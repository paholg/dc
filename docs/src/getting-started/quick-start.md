# Quick Start

<!-- OUTLINE — replace with prose. NEW CHAPTER (gap in old README): a single
end-to-end walkthrough before any of the layer detail.

- Assumes: installed, shell set up, one project configured.
- Walkthrough on a real-ish project:
  - `dc up foo -g` — what just happened (worktree created where, branch created,
    devcontainer up if configured).
  - `dc x` — you're in the container.
  - `dc status` — see it listed.
  - `dc destroy foo` — gone; cattle-not-pets philosophy in one paragraph
    (from README-old L163–166).
- "It's slow" aside → link to Cache Volumes.
- Where to go next: each guide chapter, one line each.
-->
